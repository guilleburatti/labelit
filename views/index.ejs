<!DOCTYPE html>
<html>
  <head>
      <meta name="viewport" content="width=device-width, initial-scale=1">

    <title><%= title %></title>
    <link rel='stylesheet' href='/css/bulma.css' />

    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="/js/jquery.min.js"></script>
    <script src="\javascripts\main.js"></script>

    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.13/css/all.css" integrity="sha384-DNOHZ68U8hZfKXOrtjWvjxusGo9WQnrNx2sqG0tfsghAvtVlRW3tvkXWZh58N9jp" crossorigin="anonymous">
  </head>
  <body onload="iniciar()">
        </div>
      </div>
      <h1 class="title"><%= title %></h1>

      <div class="columns">
        <div class="column">
          <div class="field">
            <div class="select is-fullwidth">
                <select name='selector' onchange="setEtiquetas(this.value);this.disabled=true;" class="desactivable">
                  <option value='0' selected disabled >Selecciona la medida de las etiquetas</option>
                  <option value='10'> 8 x 4 ( Hasta 10 etiquetas por hoja )</option>
                  <option value='2'> 16 x 8 ( Hasta 2 etiquetas por hoja )</option>
                </select>
              </div>
            </div>
            <div class="field">
                <div class="control">
                  <input class="input is-primary desactivable" type="text" id="descripcion" maxlength="32" placeholder="Nombre del producto" disabled tooltip="Ingrese el nombre del producto">
                </div>
              </div>
              <div class="field">
                <div class="control">
                  <input class="input is-info desactivable" type="number" id="precio" maxlength="4" placeholder="Precio" disabled>
                </div>
              </div>
              <button onclick="agregaProducto()" class="button is-success desactivable" id="botonAgregar" disabled>
                  Agregar
                </button>
                <button onclick="imprimir();" class="button is-primary desactivable" id="botonImprimir" disabled>
                    imprimir
                </button>
                <button onclick="reestablecer()" class="button is-danger" id="botonReestablecer">
                    Reestablecer
                  </button>
        </div>
      
        <div class="column">
          <table class="table is-striped is-bordered is-hoverable is-fullwidth">
            <thead>
              <tr>
                <th>Descripción</th>
                <th>Precio</th>
              </tr>
            </thead>
            <tbody id="tabla">
                <% for(var i=0; i<producto.length; i++) {%>

                    <tr>
                      <td><%= producto[i].descripcion %></td>
                      <td><%= producto[i].precio %></td>
                    </tr>
                    <% } %>
            </tbody>

          </table>
        </div>
      </div>
    

  </body>
</html>
