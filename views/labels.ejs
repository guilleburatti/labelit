<table>
    <% for(var i=0; i<etiquetas.length; i++) {%>
        <tr >x; width:9cm;height: 5cm; al
            <td style="border:solid; border-color: black;padding: 50; align-content: center;justify-content: center;text-align: center">
                <p>
                    <!--Descripcion-->
                    <%= etiquetas[i].descripcion %>
                </p>
                <p>
                    <%= etiquetas[i].precio %>
                    <!--Precio-->
                </p>
            </td>
        </tr>
    <% } %>
</table>
<%= JSON.stringify(etiquetas) %>
length <%= etiquetas.length %>